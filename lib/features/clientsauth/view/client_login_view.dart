import 'package:flutter/material.dart';
import 'package:hotelapp/features/clientsauth/view/widgets/client_login_view_body.dart';

class ClientLoginView extends StatelessWidget {
  const ClientLoginView({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const ClientLoginViewBody());
  }
}
