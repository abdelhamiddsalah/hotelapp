import 'package:flutter/material.dart';
import 'package:hotelapp/features/clientsauth/view/widgets/client_auth_view_body.dart';

class ClientAuthView extends StatelessWidget {
  const ClientAuthView({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const ClientAuthViewBody());
  }
}
